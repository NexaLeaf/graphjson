{
  "name": "example-pagination",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "examples/pagination",
  "tags": ["type:example"],
  "targets": {
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx examples/pagination/run.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
