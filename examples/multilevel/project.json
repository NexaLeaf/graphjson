{
  "name": "example-multilevel",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "examples/multilevel",
  "tags": ["type:example"],
  "targets": {
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx examples/multilevel/run.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["examples/multilevel/**/*.ts"]
      }
    }
  }
}
