# Default registry
registry=https://registry.npmjs.org/

# Private scope registry (example)
# @your-scope:registry=https://registry.example.com/

# Enforce auth when token exists (safe)
always-auth=true

# Performance / consistency
save-prefix='~'
fund=false
audit=false

# Authentication token - use environment variable NPM_TOKEN
# Set via: export NPM_TOKEN=your_token_here
# Or in CI: set as a secret environment variable
# //registry.npmjs.org/:_authToken=
