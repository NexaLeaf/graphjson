# json-dsl

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build json-dsl` to build the library.

## Running unit tests

Run `nx test json-dsl` to execute the unit tests via [Jest](https://jestjs.io).
