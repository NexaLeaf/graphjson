# presets

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build presets` to build the library.

## Running unit tests

Run `nx test presets` to execute the unit tests via [Jest](https://jestjs.io).
